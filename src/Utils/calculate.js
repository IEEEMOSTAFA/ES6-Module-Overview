const add = (first, second) => {
    return first + second;
}
const multiply = (first, second) => {
    return first * second;
}
const divideTheFirstNumber = (first, second) =>{
    return first * second;
}

// export default add;
// const calculator = {add: add}
// export default calculator;
export {add,multiply,divideTheFirstNumber}

